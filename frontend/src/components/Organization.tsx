import React, { FC } from "react";

const Organization: FC<any> = () => {
  return <div>single organization's homepage, should have member list, tasklists and what not</div>;
};

export default Organization;