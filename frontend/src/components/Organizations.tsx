import React, { FC } from "react";

const Organizations: FC<any> = () => {
  return <div>show all organizations that user belongs to or owns, each should link to their homepage a la tasklists</div>;
};

export default Organizations;