import React, { FC } from "react";

const Tasklist: FC<any> = () => {
  return (
    <div>
      single tasklist display and edit, need to get params from the url location
      to determine object id of this tasklist
    </div>
  );
};

export default Tasklist;
