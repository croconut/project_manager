import React, { FC } from "react";

const Tasklists: FC<any> = () => {
  return <div>tasklists display truncated list of them, links to component that can edit them</div>;
};

export default Tasklists;